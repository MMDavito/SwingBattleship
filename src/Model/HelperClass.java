package Model;

/**
 * stores global lazy variables that potentially breaks the game.
 * Delete this and all references to avoid this risk.
 */
public class HelperClass {
    public boolean ISDEBUG=false;
    public boolean CHEAT = false;//If true will give player win on first click on oponents board after gameStart.
}
